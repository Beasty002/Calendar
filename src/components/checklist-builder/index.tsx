import { ChecklistList } from './ChecklistList';

// Entry point for the checklist builder module
export const ChecklistBuilder = () => {
    return <ChecklistList />;
};

export default ChecklistBuilder;
